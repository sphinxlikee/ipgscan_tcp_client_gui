name: ipgscan_tcp_client_gui
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  window_manager: ^0.2.5
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_lints: ^2.0.1
  flutter_riverpod: ^1.0.0
  intl: ^0.17.0
  freezed_annotation: ^2.1.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  freezed: ^2.1.0+1
  build_runner: ^2.2.1

flutter:

  uses-material-design: true
